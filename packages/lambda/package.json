{
  "name": "lambda",
  "version": "0.0.1",
  "description": "Lambda function handler for remote code runner",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "test": "DEBUG=* && jest",
    "dev": "docker build -t dev-image . && docker run --rm -p 8080:8080 --env DEBUG='*' dev-image"
  },
  "dependencies": {
    "debug": "^2.6.9",
    "dotenv": "^8.2.0",
    "fp-ts": "^2.9.5"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.72",
    "@types/debug": "^4.1.5",
    "@types/jest": "^26.0.10",
    "@types/node": "^14.10.1",
    "ts-jest": "^26.4.4",
    "ts-node": "^9.0.0",
    "typescript": "^4.1.5"
  }
}
