import { Language, Template, ThemeType } from "../types";

export const DEFAULT_THEME_TYPE: ThemeType = 'light'
export const DEFAULT_LANGUAGE: Language = 'node'
export const DEFAULT_TEMPLATE: Template = 'fibo'

export const LANGUAGES: Language[] = ['node', 'python']